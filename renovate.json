{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "enabled": false,
  "description": "Disabled. This repo is artifact-only; dependency updates land on dotsecenv/dotsecenv where the plugin source lives."
}
